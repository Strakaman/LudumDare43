using System.Collections;
using UnityEngine;

public class BloodSacrifice : MonoBehaviour
{

	Animator anim;              // Ref to animator component
	GameObject player;          // Ref to player game object
	PlayerHealth playerHealth;  // Ref to player health class
	Idol idolHealth;            // Ref to Idol class
	bool playerInRange;         // Determine if player in range for sacrifice


	// Use this for initialization
	void Awake()
	{
		
	}
	
	// Update is called once per frame
	void Update()
	{
		
	}
}
